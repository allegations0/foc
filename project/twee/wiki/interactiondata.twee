:: WikiInteraction [nobr]

<<run _dom = setup.DOM.Util.filterAll({
  menu: 'interaction',
  filter_objects: Object.values(setup.interaction),
  display_callback: interaction => {
    return setup.DOM.Card.interaction(interaction);
  },
})>>
<<attach _dom>>
